---
  name: build-lib-sdjwt-swift
  on:
    pull_request:
      types: [opened, reopened]
    push:
      branches: ['main']
      tags: [ v* ]
  jobs:
    build:
      runs-on: "macos-13"
      steps:
        - uses: actions/checkout@v4
        - run:
            fastlane tests